import { metadata } from './metadata';

export { metadata };

export default function BusinessFinanceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      {children}
    </>
  );
} 