import { metadata as pricingMetadata } from './metadata';

export const metadata = pricingMetadata;

export default function PricingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
